<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tasteforming.mapper.InsertMapper">
	<insert id="insert">
		insert into member (member_id, password, role, name, address)
		values (#{member_id},#{password}, #{role}, #{name}, #{address})
	</insert>
</mapper>
